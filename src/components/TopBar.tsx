import{Menu,Sparkles}from'lucide-react';import{useState}from'react';import type{Balance}from'../types';import ProfileMenu from './ProfileMenu';
export default function TopBar({balance,onAdd}:{balance?:Balance;onAdd:()=>void}){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-10 mx-auto flex max-w-7xl items-center justify-between gap-3 p-4"><div className="flex items-center gap-3"><Menu className="md:hidden"/><Sparkles className="text-purple-400"/><b className="text-2xl">Sartrix</b></div><div className="flex items-center gap-2 text-sm"><span className="hidden rounded-xl border border-white/10 bg-white/5 px-3 py-2 sm:block">✨ Free generations: {balance?balance.free_generations_total-balance.free_generations_used:2} / {balance?.free_generations_total??3}</span><span className="rounded-xl border border-white/10 bg-white/5 px-3 py-2">⚡ {balance?.credits_balance??125} credits</span><button onClick={onAdd} className="neon rounded-xl px-4 py-2 font-semibold">Add credits</button><div className="relative"><button onClick={()=>setOpen(!open)} className="h-10 w-10 rounded-full bg-gradient-to-br from-orange-300 to-purple-500">👩</button>{open&&<ProfileMenu/>}</div></div></header>}
