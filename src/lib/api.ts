import type { Balance, GenerationJob, GenerationMode, GenerationSettings, UploadedFile } from '../types';
const j=async<T>(url:string,init?:RequestInit):Promise<T>=>{const r=await fetch(url,init); if(!r.ok) throw new Error((await r.json().catch(()=>({error:r.statusText}))).error||'Request failed'); return r.json()};
export const api={upload(files:File[]){const f=new FormData();files.forEach(x=>f.append('files',x));return j<UploadedFile[]>('/api/uploads',{method:'POST',body:f})},generations(){return j<GenerationJob[]>('/api/generations')},generation(id:string){return j<GenerationJob>(`/api/generations/${id}`)},createGeneration(body:{mode:GenerationMode;prompt:string;uploaded_files:UploadedFile[];settings:GenerationSettings;rights:boolean;faceSafety:boolean}){return j<GenerationJob>('/api/generations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})},deleteGeneration(id:string){return j<{ok:boolean}>(`/api/generations/${id}`,{method:'DELETE'})},favorite(id:string){return j<GenerationJob>(`/api/generations/${id}/favorite`,{method:'POST'})},retry(id:string){return j<GenerationJob>(`/api/generations/${id}/retry`,{method:'POST'})},assistant(body:{mode:GenerationMode;message:string;currentPrompt:string}){return j<{suggestion:string}>('/api/prompt-assistant',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})},balance(){return j<Balance>('/api/balance')},topUp(credits:number){return j<Balance>('/api/billing/mock-top-up',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({credits})})}};
