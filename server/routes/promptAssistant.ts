import{Router}from'express';export const promptAssistantRouter=Router();promptAssistantRouter.post('/',(req,res)=>{const{mode,message,currentPrompt}=req.body;const base=currentPrompt||message||'Create a polished result';const map:any={'Photo to Video':`${base}. Add subtle head movement, natural blinking, gentle camera motion, realistic hair movement, and cinematic lighting for a smooth short video.`,'AI Photoshoot':`${base}. Create an editorial AI photoshoot with premium styling, flattering light, detailed wardrobe direction, and a clean professional background.`,'Enhance Photo':`${base}. Enhance clarity, restore detail, improve color balance, reduce noise, sharpen gently, and keep the subject natural.`,'Face Swap':`${base}. Produce a realistic, consent-based face swap with natural skin tone matching, lighting consistency, and no deceptive or harmful context.`};res.json({suggestion:map[mode]||`${base}. Make it cinematic, detailed, and high quality.`})});
